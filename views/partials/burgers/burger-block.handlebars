<li>
	{{burger_name}}

	{{!-- <button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button> --}}
    {{#if devoured}}
        <button  class="eat-burger main-btn" data-id="{{id}}">
            DEVOUR BURGER
        </button>
    {{/if}}
</li>
